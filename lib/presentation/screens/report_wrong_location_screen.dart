import 'package:flutter/material.dart';

class ReportWrongLocationScreen extends StatelessWidget {
  const ReportWrongLocationScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('ReportWrongLocation Screen',style: TextStyle(color: Colors.pink),),
      ),
    );
  }
}
